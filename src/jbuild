(library
 ((name spawn)
  (public_name spawn)
  (c_names (spawn_stubs))
  (c_library_flags (-lpthread))
  ;; No deps outside of the compiler distribution!
  (libraries (unix))
  (preprocess no_preprocessing)))

(jbuild_version 1)
